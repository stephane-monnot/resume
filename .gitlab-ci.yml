image: node:latest

stages:
  - deploy

before_script:
  - yarn

pages:
  stage: deploy
  environment:
    name: production
    url: https://stephanemonnot.com/
  script:
      - yarn build
      - mv public oldpublic
      - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
